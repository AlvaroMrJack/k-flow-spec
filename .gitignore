# Binaries
bin/
*.exe
*.exe~
*.dll
*.so
*.dylib
kfs
k-flow-spec

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (released as part of the module)
vendor/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Env files (user-specific secrets)
.env
.env.local

# KFS artifacts (generated — not committed)
kfs-snapshots/
kfs-reports/
kfs-mock-fixtures/
